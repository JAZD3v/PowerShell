This script gets all permissions from a site and exports them to a CSV file. It also pulls users from the SharePoint groups and list them individually by the permission level of the group. It only list the highest permission level of the user. EXAMPLE: If a user was added individually weith contribute, but they are in the owners group that has full Control, then the permission Level for the user will be Full Control.
